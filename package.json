{
  "name": "lens-iq",
  "private": true,
  "packageManager": "pnpm@10.0.0",

  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },

  "devDependencies": {
    "tsup": "^8.5.1",
    "tsx": "^4.22.1",
    "turbo": "^2.9.14",
    "typescript": "^5.9.2"
  },

  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/engines",
      "esbuild",
      "prisma",
      "sharp"
    ]
  }
}